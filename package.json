{
  "name": "camp_courses",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "live-server public --port=3000 --entry-file=index.html"
  },
  "dependencies": {
    "lite-server": "^2.6.1",
    "live-server": "^1.2.2"
  }
}
